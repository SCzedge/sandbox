spring:
  batch:
    job:
      enabled: false
  #Mariadb
  datasource:
    hikari:
      driver-class-name: org.mariadb.jdbc.Driver
      jdbc-url: jdbc:mariadb://localhost:3306/batch?serverTimezone=UTC&characterEncoding=UTF-8&useSSL=false
      username: root
      password: dbghtmxm1!
